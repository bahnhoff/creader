---
declared-services:
  conversation-experimental:
    label: conversation
    plan: experimental
applications:
- services:
  - Conversation-1
  command: npm start
  path: .
  instances: 1
  memory: 512MB
